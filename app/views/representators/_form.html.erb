<%= form_with(model: representator, local: true) do |form| %>
  <% if representator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(representator.errors.count, "error") %> prohibited this representator from being saved:</h2>

      <ul>
      <% representator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :tel %>
    <%= form.text_field :tel %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
